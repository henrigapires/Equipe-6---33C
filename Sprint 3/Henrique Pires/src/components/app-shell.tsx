import { Link } from "@tanstack/react-router";
import { CircleUserRound,Menu } from "lucide-react";
import { useState,type ReactNode } from "react";
import { Brand } from "./brand";
const links=[{to:"/minha-grade",label:"Minha grade"},{to:"/grades-sugeridas",label:"Grades sugeridas"},{to:"/questionario",label:"Questionário"},{to:"/ajuda",label:"Ajuda"}] as const;
export function AppShell({children}:{children:ReactNode}){const [open,setOpen]=useState(false);return <div className="min-h-screen bg-background text-foreground"><header className="sticky top-0 z-40 border-b border-border bg-surface/95 backdrop-blur"><div className="mx-auto flex h-20 max-w-[1440px] items-center justify-between px-5 lg:px-10"><Link to="/minha-grade" aria-label="Ir para minha grade"><Brand compact/></Link><nav className="hidden items-center gap-9 lg:flex">{links.map(l=><Link key={l.to} to={l.to} className="font-display text-sm text-muted-foreground transition-colors hover:text-primary" activeProps={{className:"nav-active"}}>{l.label}</Link>)}</nav><div className="flex items-center gap-3"><CircleUserRound className="text-primary" size={30}/><span className="hidden font-display text-sm sm:inline">Olá, Maria</span><button className="lg:hidden" aria-label="Abrir menu" onClick={()=>setOpen(!open)}><Menu/></button></div></div>{open&&<nav className="grid border-t border-border bg-surface px-5 py-3 lg:hidden">{links.map(l=><Link key={l.to} to={l.to} onClick={()=>setOpen(false)} className="py-2 font-display text-sm">{l.label}</Link>)}</nav>}</header>{children}<footer className="mt-auto border-t border-border bg-surface"><div className="mx-auto flex max-w-[1440px] items-center justify-between px-5 py-5 text-xs text-muted-foreground lg:px-10"><b className="font-display text-primary">grade.puc</b><span>Planejamento hoje. Mais conquistas amanhã.</span><span className="font-display">PUC-Rio</span></div></footer></div>}
